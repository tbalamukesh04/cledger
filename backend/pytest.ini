[pytest]
pythonpath = .
testpaths = tests/unit tests/integration tests/failure tests/performance
markers = 
    unit: marks pure logic unit tests
    integration: marks integration tests
    failure: marks failure simulation tests
    performance: marks load and stress performance tests
    asyncio: marks tests as async

asyncio_mode = auto
asyncio_default_fixture_loop_scope = function